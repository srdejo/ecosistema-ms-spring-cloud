server:
  port: 8099
  wait-time-in-ms-when-sync-empty: 0

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

      